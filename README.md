# UpRock Headless Linux Installer

Installs UpRock Mining on a headless Debian/Ubuntu machine using Xvfb and systemd.

## Install

Run as root:

```bash
chmod +x install-uprock-headless.sh
./install-uprock-headless.sh
